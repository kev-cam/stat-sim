# nvc work library / elaboration output
build/

# Python bytecode
__pycache__/
*.pyc

# waveform dumps are committed deliberately as demo artifacts (test/*.fst),
# but ignore ad-hoc ones elsewhere
/*.fst
/*.vcd
